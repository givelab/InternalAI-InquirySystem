[tool.poetry]
name = "frontend"
version = "0.1.0"
description = "Streamlit frontend for backend API"
authors = ["yoshiaki.kakui@abejainc.com"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
streamlit = "^1.41.1"
requests = "^2.32.3"
python-dotenv = "^1.0.1"
pandas = "^2.2.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
